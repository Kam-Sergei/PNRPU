јнализ:
“ак как n не указано, то пользователь должен ввести количество элементов в 
последовательности (n). “ак как последователность начинаетс€ с 1, то
введЄм переменную int i=1. ¬ведЄм переменную float max=0, в которую будет
присваиватьс€ максимальное значение последовательности. »спользуем цикл
while (i<=n), в котором будет вычисл€тс€ элемент последовательности
a = sin(n + i / n) , после чего будет провер€тьс€ €вл€етс€ ли элемент 
последовательности первым (if (i == 1)), если €вл€етс€, то присваиваем
max = a и j = i, где j-номер максимального элемента, иначе сравниваем 
значение этого эемента с максимальным (else if (a > max)), если условие 
выполн€етс€, то присваиваем max = a и j = i. ѕосле проверок переходим к 
следующему элементу последовательности, дл€ этого прибавл€ем к i 1 (i++).
ѕосле выполнени€ цикла ,так как надо найти максимальный элемент в 
последовательности (его значение и номер), выведем номер максимального
значени€ в последовательности (j) и максимальное значение в последовательности
(max).